pipeline {
    agent {
        docker { image 'openjdk:17.0.2' }
    }
    stages {
        stage("Start") {
            steps {
                echo 'Start spring boot project'
            }
        }
    }
}
